class Week {

    static dayOfWeek():string{
        return "Today is not the same as Yesterday!"
    }
}

// For static methods they do not require an instance of a class but rather you call it with the class name itself.

Week.dayOfWeek();